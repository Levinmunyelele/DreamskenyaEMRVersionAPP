import { ComponentFixture, TestBed } from '@angular/core/testing';
import { AgywGraduationPage } from './agyw-graduation.page';

describe('AgywGraduationPage', () => {
  let component: AgywGraduationPage;
  let fixture: ComponentFixture<AgywGraduationPage>;

  beforeEach(() => {
    fixture = TestBed.createComponent(AgywGraduationPage);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
